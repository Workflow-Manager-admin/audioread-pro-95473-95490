/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}G/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}_/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}K/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}X/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}U/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}|/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}|/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}5/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}^/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}?/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}?/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}|/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}|/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}3/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}G/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}_/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}K/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}G/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}_/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}K/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}?/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}G/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}_/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}K/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}?/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}G/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}_/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}K/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}5/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}5/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}?/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}\/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}W/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}H/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}G/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}_/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}K/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}'/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}5/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}|/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}|/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}!/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}z/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}M/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}G/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}U/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}_/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}T/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}R/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}D/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}S/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}N/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}F/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}w/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}A/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}@/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}O/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}b/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}j/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}*/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}p/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}C/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}B/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}y/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}+/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}[/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}]/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}(/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}>/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}&/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}</**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
})/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}m/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}L/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}=/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}E/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}//**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}f/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}{/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}I/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}d/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}x/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}c/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}u/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}k/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}./**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}g/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}h/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}-/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}1/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
},/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}r/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}l/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}a/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}v/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}e/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}P/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}s/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}t/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}i/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}o/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}n/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}:/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}0/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
} /**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}}/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
};/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}
/**
 * Split text into an array of word spans and non-word spans with char offsets.
 * This is the canonical routine to be used for both rendering (highlight sync) and TTS boundary mapping.
 * 
 * @param {string} text - The text to split
 * @param {number} [startOffset=0] - The starting global char offset
 * @returns {Array<{text: string, offset: number, word: boolean}>}
 */
// PUBLIC_INTERFACE
export function splitTextToWordSpans(text, startOffset = 0) {
  // Robust: words, apostrophes, dash, punctuation
  const pattern = /([A-Za-z0-9\'’\-]+|[^\w\s]+)/g;
  const result = [];
  let match;
  let currentOffset = startOffset;
  let lastIndex = 0;

  while ((match = pattern.exec(text)) !== null) {
    if (match.index > lastIndex) {
      // Push whitespace (non-clickable/non-word)
      const ws = text.slice(lastIndex, match.index);
      result.push({ text: ws, offset: currentOffset, word: false });
      currentOffset += ws.length;
    }
    const wordText = match[0];
    const isWord = /^[A-Za-z0-9\'’\-]+$/.test(wordText);
    result.push({ text: wordText, offset: currentOffset, word: isWord });
    currentOffset += wordText.length;
    lastIndex = match.index + wordText.length;
  }
  // Push trailing whitespace if any
  if (lastIndex < text.length) {
    result.push({
      text: text.slice(lastIndex),
      offset: currentOffset,
      word: false
    });
  }
  return result;
}